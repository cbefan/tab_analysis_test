I have the analysis split into two parts. The first calculates the data and dumps them into pickles (the \*_pickle.py). The other generates chemiscopes (\*_chemiscope.py)

run with "pixi run -e calc python"
